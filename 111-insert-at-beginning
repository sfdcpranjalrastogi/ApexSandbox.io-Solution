public void insertAtStart(String cityName, List<String> cities) {
    if(cityName != null)
    {
        Integer i = cities.size();
        while(i >= 0)
        {
            cities[i+1] = cities[i];
            i--;
        }
        cities[i] = cityName;
    }
}