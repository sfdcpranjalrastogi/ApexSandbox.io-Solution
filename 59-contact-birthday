public Boolean isBirthday(Contact c) {
    Integer todayDay = Date.day(System.today());
    Integer todayMonth = Date.month(System.today());
    Integer dayy = Date.day(c?.Birthdate);
    Integer monthh = Date.month(c?.Birthdate);

    return (todayDay == day && todayMonth == monthh);
}